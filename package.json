{
  "name": "innate",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @innate/web dev",
    "run:project": "bash run.sh",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "clean": "rm -rf .next node_modules && pnpm -r clean",
    "sync": "pnpm --filter @innate/task-watcher task-watcher sync"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  },
  "packageManager": "pnpm@10.33.0"
}
