# Source: https://betterstack.com/community/guides/scaling-docker/podman-compose/
# Original language: yaml
# Normalized: yaml
# Block index: 12

[label docker-compose.yml excerpt]
environment:
  POSTGRES_USER: ${POSTGRES_USER}
  POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
  POSTGRES_DB: ${POSTGRES_DB}