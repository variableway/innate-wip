name: Deploy writing to GitHub Pages

# 本仓自己的静态站。不 clone 整个 fe-templates，只 sparse 拉 @innate/ui。
on:
  push:
    branches: [main, master]
    paths:
      - "use-cases/**"
      - "src/**"
      - "index.html"
      - "package.json"
      - "vite.config.ts"
      - ".github/workflows/deploy-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout this site
        uses: actions/checkout@v4
        with:
          path: site

      - name: Sparse checkout @innate/ui
        uses: actions/checkout@v4
        with:
          repository: ${{ vars.FE_TEMPLATES_REPO || 'variableway/innate-fe-templates' }}
          token: ${{ secrets.FE_TEMPLATES_TOKEN || github.token }}
          path: templates
          sparse-checkout: |
            packages/ui
            packages/tsconfig
            package.json
            pnpm-workspace.yaml
            pnpm-lock.yaml
          sparse-checkout-cone-mode: false

      - uses: pnpm/action-setup@v4
        with:
          version: 12

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: templates/pnpm-lock.yaml

      - name: Install and build
        working-directory: templates
        run: |
          mkdir -p apps
          rm -rf apps/innate-wip
          mv ../site apps/innate-wip
          pnpm install --filter @innate/wip...
          PUBLIC_PATH=./ pnpm --filter @innate/wip build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: templates/apps/innate-wip/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
